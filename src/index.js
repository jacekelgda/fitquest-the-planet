const { fetchUsersFromChannel } = require('./services/slack/client');
const { decodeUsernames } = require('./util/slack');
require('dotenv').config();

const dealDamage () => {
  // fetch all participants
  // do -1HP to whatevel HP valut
}
